I'll verify the frozen state first, then map the web surface.Target matches frozen commit; workspace matches too. One dirty file in target — checking, then reading core files.Untracked file is only `.ultraplan/` (tool state), not source. Reading the routing/security/server layer now.Core routing/security captured. Now reading the projection handlers and artifact pipeline.Now the remaining handler files and the operations engine.Some lines in that read look garbled (transit artifact). Re-reading the suspect ranges to get exact content.Clean reads confirmed. Now the operations engine and its handlers.Web package mapped. Now the app-layer dependency surface: web_usecases, markdown sanitizer, and artifact refs.Now wiring (cmd → server), and the authoritative workspace contracts for this surface.Contract captured. Now composition wiring and remaining assets/tests inventory.Checking artifact conventions before writing the pack.All evidence gathered. Verifying one last wiring detail, then writing the pack.Writing the context pack to the review artifacts directory.Context pack complete: `review/surfaces/context-web-routing-projection.md` (147 lines, 11 sections).

**What it covers, grounded in the frozen target:**

- **Control flow**: composition (`cmd/ultraplan/main.go:30` → `app/serve_commands.go` → `web.Run`), the per-request chain (security middleware rejection order → `matchRoute` → method allowlist → `dispatch`), and a full route table for the ~48 hand-routed endpoints.
- **Inputs/outputs**: per-route query allowlists, identifier vs opaque-ref regexes (including that all params on `*artifact*` routes use the stricter ref pattern), strict JSON/form decoding, `{data,meta}`/`{error,meta}` envelopes with collection truncation triad.
- **State**: authoritative state stays in app/workspace/run-journal; process-lifetime transport state inventoried (HMAC session secret, preparation store, operation hub, artifact ref mint map — refs die on restart).
- **Trust boundaries**: Host/Origin/CSRF/session gates incl. the port-less Origin fallback and static-asset exemption; markdown only reaches HTML via `RenderSafeMarkdown`; artifact containment double-checked post-symlink-resolution.
- **Error/restart semantics**: QA/run-control/operation error taxonomies, cursor conflict/gap handling, Retry-After + dedup retry, restart fallbacks to durable journal.
- **Contracts**: frozen baseline (`api_compatibility_test.go`, `docs/web-compatibility-baseline.md`), TRD §18A, Sprint 30 api-design reasoning; dependency surface = shared-usecase-vocabulary capability interfaces.
- **Unknowns flagged neutrally** for reviewers: SSE streams without extended write deadlines vs 30s server WriteTimeout, rand-failure deterministic-secret fallbacks, unbounded ref-map growth, timeline tool-key producer contract, trailing-slash/Host edge behavior.