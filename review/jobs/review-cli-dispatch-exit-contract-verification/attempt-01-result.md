Context pack read. The stated frozen commit `50d6f0d` is the current HEAD, so the working tree is authoritative. Setting up a plan and reading primary sources.Dispatch core read. Now reading the command grammars in parallel — starting with the largest risk areas.Noted several candidates already (notably: the `flow` JSON-path classification ladder skips the `runtime` substring branch that the non-JSON path applies). Continuing through the file and related sources.`durable_operations.go` read — noting the `Finish`-failure join behavior interacting with the substring ladders. Now reading status_json.go, usecases.go, and the remaining command files.The substring producers are coming into focus. Now I need to check `Execute`'s cancellation path — whether a cancelled execute can return `nil` error and exit 0.`hasFailedExecuteTask` includes cancelled tasks — so execute-under-cancellation exits 8, not 7. Now checking `RefError` message shapes (do they embed absolute paths?) and how `Flow` surfaces errors, to test the substring-sniffing blast radius.RefError messages embed candidate names (user-controlled directory names) — that confirms a concrete substring-injection vector into the `"runtime"` sniffers. Now reading the remaining foundation commands and run_control state, then the tests.Now the tests and docs. Reading sprint_commands_test.go sections that pin flow behavior and the CLI reference contract.The test pins exit-6 for a flow persistence failure without `--json`. Now I need to establish which real producer errors actually carry the `"runtime"` substring to judge reachability of the JSON/non-JSON divergence.Tests mapped. Now reading the authoritative contract doc, then building the binary for empirical probes.Docs confirm a global 0–8 table ("6: runtime/provider error", "5: validation/reference error"). Now empirical probes. First, checking what makes a discoverable project/sprint so I can craft the RefError fixture.Directory names are free-form safe-names — fixture confirmed viable. Building the binary for probes.**Finding 1 empirically confirmed** — the same reference error yields three different exit classes: `status`=5, `flow` (text)=6, `flow --json`=5. The `"runtime"` substring sniffed from a *sibling directory name* hijacks classification, and adding `--json` changes the class again. Next probes: serve usage-stream discipline and the storage/test-coverage gaps.Two more findings confirmed empirically. Now checking whether the `sprint status --json` PascalCase result fields match the phase3 schema contract — it's a declared stable surface.The `sprint.StatusSummary` has proper tags (my probe hit `FlowResult`, which lacks tags — flow isn't a stable surface, so that's fine). New lead: `mapQACommandError`'s fallback may demote the designed ExitRuntime of accept/finish failures to 4, contradicting the documented QA mapping. Probing.QA persistence failure exits **4** with code `validation.workspace` while `run list` exits **6** on the identical fault — and cli-reference.md:363 promises 6 for QA. Now probing the `verify/review --json` failure envelopes' `status` field on declared-stable surfaces.`sprint review` with the same fixture exits **6** (second substring site confirmed live), and `verify --json` emits `"status": ""` — an undefined value on a declared-stable surface. Now checking test counter-evidence and running the suite baseline.All evidence gathered. Grabbing exact doc line numbers for citations, then writing up.Review complete. Findings written to `review/jobs/review-cli-dispatch-exit-contract/result.md`.

**5 findings, 3 live-reproduced against a binary built from the frozen tree:**

- **F1 (high):** The `"runtime"`/`"failed tasks"` substring ladders (`sprint_commands.go:269-272,360-365,507-509`) misclassify on operator-controlled text. One fixture (`projects/runtime-tools/` + missing ref) produced **exit 5 via `status`, exit 6 via `flow`, exit 5 via `flow --json`, exit 6 via `review --json`** — the flow JSON branch omits the substring check entirely, so `--json` changes the exit class.
- **F2 (medium-high):** Durable accept/finish failures are built as `ExitRuntime` (`durable_operations.go:57,81`) but demoted to **exit 4 / `validation.workspace`** by fallback mappers. Live: `qa cancel/resume` with a broken run-control store → 4; identical fault via `run list` → 6. Contradicts cli-reference.md:363's "runtime or persistence failures 6" on a declared-stable QA surface.
- **F3:** SIGINT during `sprint execute` exits **8**, not 7 — cancelled tasks count as "failed tasks" (`execute.go:296-298,750-757`) and the text branch pre-empts cancellation mapping. Undocumented divergence from flow/study.
- **F4 (low):** `serve --bogus` prints 640 bytes of help to **stdout** plus two stderr lines; every other verb emits nothing on stdout and one stderr line. Tests pass nil streams so can't see it.
- **F5 (low):** Declared-stable `sprint verify --json` emits `"status": ""` when Verify fails early — out-of-vocabulary value on a compatibility-promised surface.

Also recorded: verification gaps (zero storage-migrate tests; inventory test greps source, not behavior) and 8 defended non-issues with counter-evidence. Baseline suite was green throughout — none of these are caught by current tests.