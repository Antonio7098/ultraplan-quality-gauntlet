1. Written: `review/surfaces/context-opencode-agent-runtime.md` — 175 lines.

2. Coverage summary:
Surface = internal/platform/runtime (9 source + 5 test files, all read) plus the pinned agentwrap SDK v0.0.0-20260825130518-dccd575bd101 from the module cache. Entrypoints: Adapter.StartRun (runtime.go:310-419), Health/Capabilities/ListModels, DeleteSession(s)/DeleteRuntimeStore hooks wired only by NewOpenCode (opencode.go:31-127). Mapped mechanisms: Request→RunRequest mapping with whitelisted health/capability names and permission-policy validation (agentwrap.go:9-101); store-path/cache-cohort metadata injection (runtime.go:576-597). Binary invocation from SDK reality: `opencode run --format json`, stdin prompt, process-group TERM→KILL teardown, OPENCODE_DB isolation + snapshots-disabled env (agentwrap opencode/runtime.go:127-168, process_linux.go). Event ring keeps last 200 with 64-field/8KiB/depth-3 sanitize bounds and raw-payload omission records (runtime.go:144,:450-470; agentwrap.go:121-225). Policy: DefaultRetries+1 attempts, 1s→30s exponential backoff, rate-limit retries, backup-model fallback whose overlay preserves OPENCODE_DB metadata (opencode.go:52-72; agentwrap policy.go:143-276,:776-831); local missing-session stop policy. Cancellation: run.Cancel + ≤5s grace → synthesized eventless cancelled result abandoning teardown observation (runtime.go:362-381). Deletion: interpolated SQL via `<exe> db` behind quote-doubling sqliteString, `<exe> session delete`, checkpoint×20/VACUUM/checkpoint (opencode.go:93-188). Stores: sha256(owner)[:16] hash dirs, store.json active/retained/cleanup_pending, dead-PID→retained GC, RemoveAll under containment marker, XDG log pruning 48h/128MiB (store.go, opencode_maintenance.go). Consumers traced to study/sprint/app/health call sites with file:line. Contracts: TRD §11.1-11.4/§12.3, ARCHITECTURE placement rules, sprints 09/12/26 quoted by line. Tests evidence map covers all 24 test functions incl. cache_test.go's note that its code-under-test lives in runtime.go:583-597. Explicit unknowns listed: 12.

3. Blockers/gaps: none material. The full agentwrap SDK was present in the module cache at the exact pinned pseudo-version (all cited symbols verified); the external `opencode` binary itself is not inspectable, recorded as unknown #2 rather than guessed. No CURRENT-CONTRACT doc covers store.json/deletion machinery — recorded as unknown #1 without verdicts. Pre-existing untracked artifacts in the gauntlet repo (review/jobs/*, state.json) were left untouched; only the one required file was written.
