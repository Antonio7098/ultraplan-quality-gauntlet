The primary-path files are identical between the frozen commit and HEAD except `qa_state.go` (QA surface); I'll verify against `c6f01cf` where relevant. Setting up my review plan.Core state files read. Now the lock/cleanup/artifact files and domain types.Now let me verify the DB load path assumptions — item ordering and what the migrate command imports.This is important — let me trace `codeContextPrerequisite` and how a persisted complete code-context stage gates regeneration.Let me check what ArtifactSnapshot validation actually covers, and the remaining flow functions' prerequisite ordering.Now the tests. Let me read the key pinned tests for this surface.Repro environment ready at the frozen commit. Writing demonstration tests for the top candidates.The F1 demonstration shows the stale save reverting completed evidence (`FAIL` is the proof). Final checks: does `PrepareReview` (called inside Status before its save) write anything, and does the smoke writer share the same blind-RMW pattern?I have my evidence. Let me check the expected output location for this job.One last check for F2's downstream impact: what gates read persisted planning-stage completion?All evidence gathered. Writing the findings document.Review complete. Findings written to `review/jobs/review-sprint-flow-state-verification/result.md`, with runtime demonstrations on a scratch clone of the frozen commit (`/tmp/opencode/ulp-repro`, target untouched).

**5 findings:**

1. **F1 (Medium-High)** — `Service.Status` saves refreshed flow state without the mutation lease (`service.go:291`); demonstrated that a stale snapshot save reverts completed review evidence (`LastComplete` → running attempt). Reachable via TUI refresh during operations (`readOnly` unset, `ActionRefresh` unguarded) or CLI status vs. daemon writers.
2. **F2 (Medium)** — Crash between `O_EXCL` create and pidfile write leaves an unparseable lock; acquisition fails closed forever, reconcile silently skips it (`(false,nil)`), no CLI remediation. Demonstrated permanent brick from empty/partial JSON.
3. **F3 (Low-Medium)** — Package-level `flowFailedStages` (`flow.go:385`) fabricates prior-stage completions without reading state/artifacts — including persisting `requirements=complete` in the same branch that just failed requirements validation, and erasing prior failure diagnostics.
4. **F4 (Low-Medium)** — Startup reconciliation aborts the whole workspace on the first bad sprint and refuses web server start (`server.go:76-81`), while presentation treats identical conditions as per-sprint findings.
5. **F5 (Low)** — `TestLegacyV2ReaderClassifiesPublishedQASummaryAsMalformed` never calls `LoadFlowState`; it asserts on its own inline decoder — vacuous coverage for a pinned load-grammar contract.

**Defended non-issues** recorded: DB-branch leniency (import path guarantees strict v2 rows by construction), DB-checkpoint partial failure semantics, EPERM-as-alive, release tuple equality, read-time-only migrations, marker ordering, discarded save errors, ignored dir-fsync.