Context pack read. Now let me verify the frozen commit and set up an inspection copy.Fresh export at `/tmp/opencode/cli-review`. Reading core dispatch files now.Noted several candidates. Reading the rest of sprint_commands.go.Nil-safety confirmed. Now I see a candidate defect: `finishDurableCLICommand` joins an `ExitRuntime`-classed finish error, but downstream ladders re-wrap via `mapSprintError` (default class 4), losing the runtime class. Let me verify the error-text path and check study commands.Now reading the authoritative CLI contract doc.Doc confirms class semantics (`4` filesystem, `6` runtime/persistence). Now reading remaining command families.RefError embeds the raw user-supplied reference in its message — confirming the substring-sniffing false-positive path. Checking the phase3 contract and `displaySafe`, then tests.Now let me build the binary from the frozen snapshot and empirically reproduce the two leading candidates. First, checking what a minimal workspace needs.Direct hit — exit 6 for a missing-artifact error. Now the control group without "runtime" in the name:Text-mode flow misclassifies purely on the project name (4→6), while JSON mode of the same command stays 4. Now testing the same poison name against `review`/`execute`, then proving the finish-failure demotion with a scratch test inside the exported copy.`review` and `execute` confirmed misclassifying (4→6); `verify` is immune (typed mapper). Now proving the finish-persistence demotion (candidate F1) with a scratch test exercising the real classifiers:Both leading findings proven. Now testing the third candidate: QA routing acceptance/open failures through `mapQACommandError` (demoting runtime→4) while sibling verbs keep 6. Corrupting the run-control DB to force open failure:Odd double stderr line on study run-all — isolating it:Single clean line — earlier doubling was shell interleaving. Now checking existing tests for counter-evidence that might pin these behaviors as intentional:The TUI/web adapter (`failedOperation`) deliberately avoids text-sniffing — the CLI ladders contradict the codebase's own classification doctrine. Final probes: serve stderr behavior and the health `filesystem.read` claim:Both confirmed. Quick sweep of the two remaining files, then the inventory test:One more adversarial pass: `mapRunControlError` has no cancellation arm — checking reachability through the `run` door and probing the classifier directly:`classifyStoreError` passes raw `context.Canceled` through, so the gap is reachable. Completing the flow JSON/text matrix for the report:All verification complete. Writing the findings to the review artifacts directory:Review complete. Findings written to `review/jobs/review-cli-dispatch-exit-contract-adversarial/result.md`. Five defects, all verified against the frozen binary (`50d6f0d`) plus probe tests; none pinned by existing tests.

**F1 — Finish-persistence failures demoted 6→4 (medium, high conf).** `Finish()` classes DB failure as ExitRuntime (durable_operations.go:81), but every handler re-wraps via mappers defaulting to ExitWorkspace(4) (sprint_commands.go:961, study_commands.go:958). Proven for all six verb families. Worse: corrupt run-control DB → `run list`=6 but `sprint qa run/cancel --json`=**4** with envelope code `validation.workspace` — directly contradicting cli-reference.md:363 ("runtime or persistence failures 6").

**F2 — Substring classification sniffs user-controlled names (medium-high, high conf).** `strings.Contains(err.Error(), "runtime")` (sprint_commands.go:269/363/507) matches project names and workspace paths. Empirical, identical failure differing only in name: `sprint demo s1 flow --to plan --dry-run`→**4**, `sprint runtime-demo ...`→**6**; same for execute and review; JSON mode of the same command→4. The TUI/web classifier refuses text-sniffing by design (operations.go:751-796 + sprint_error_test.go guard) — the CLI ladders contradict in-repo doctrine.

**F3 — `mapRunControlError` has no cancellation arm (medium).** SIGINT landing inside a `run *` query exits 6 instead of 7; `run follow` can exit 6 mid-poll despite its documented clean-stop guarantee (idle-path deliberately returns 0 at run_commands.go:188-193). runcontrol passes `context.Canceled` through raw.

**F4 — `health` `filesystem.read: ok` is hardcoded (low-medium).** health_commands.go:65 never reads anything; demoed with chmod-000 config: check reports ok alongside a permission-denied config failure. Doc claims readability checking.

**F5 — `serve` usage errors dump 15 help lines to stdout + duplicate error on stderr (low).** Every other verb emits nothing to stdout on usage errors.

Defended non-issues (QA cancellation→8 asymmetry, empty alias digest, nil durable handling, `--yes` gate ordering, status-label semantics, etc.) are documented in the result file with evidence and repro artifacts under `/tmp/opencode/cli-review` and `/tmp/opencode/ws-rt`. Target repo untouched.