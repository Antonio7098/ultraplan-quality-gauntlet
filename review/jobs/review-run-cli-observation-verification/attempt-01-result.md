Context pack read. Target files match frozen commit (diff empty for reviewed paths). Now reading the use-case adapter, tests, and the runcontrol store internals these commands project.Store internals read. Now checking app.go dispatch/exit handling, model/id/cursor definitions, retention normalization, and the web follow handler for the replay-parity comparison.Web parity confirmed (`cursor_ahead`/`replay_gap` exist there, not in CLI). Now checking the authoritative TRD wording for the replay-gap requirement, and config validation of run-control durations.TRD wording confirmed: "an unavailable cursor produces a typed retention-gap result ... rather than silently beginning a partial stream" and clients must "distinguish complete replay from a gap". Now reading the tail of app.go (error/exit handling), loadEffectiveConfig, metrics, and local log reader.`config.Load` validates run-control durations upstream (config.go:175, 487–500) — the parse-error swallow in `run_control.go:154-156` is unreachable dead defensiveness. Now I'll empirically verify the two follow-loop hypotheses against a scratch copy of the repo.Test harness understood (`Run(Config{Context})` accepts a custom context). Setting up a scratch copy to verify the follow-loop hypotheses empirically without touching the target tree.Finding A empirically confirmed: follow began at sequence 257 of 2300 with zero gap indication, exit 0. Running B and C:Both remaining hypotheses confirmed. Quick final checks: is `health.Diagnostics` ever printed in text mode anywhere, and what's the job-result convention in the review tree?Job directory confirmed. Before writing results, establishing the baseline test claim in the scratch clone:Baseline green. Writing the review result:Review complete. Results written to `review/jobs/review-run-cli-observation-verification/{attempt-01-result.md,result.md}`. Target tree untouched; all reproductions ran in a scratch clone (since removed), baseline tests green.

**Findings:**

- **F1 (Medium, high confidence, reproduced)** — CLI `follow` has no retention-gap or cursor-ahead signal (run_commands.go:161-194). On a compacted non-terminal run, `--after 0` silently began replay at sequence **257 of 2300**, streamed to exit 0 with no marker; `--after 999999` polled forever with zero output. TRD §12.3 forbids "silently beginning a partial stream" and the web door implements typed `cursor_ahead`/`replay_gap` (run_handlers.go:968-980) — the CLI diverges from both contract and sibling surface, and no test pins it.
- **F2 (Low, mechanism verified)** — Ctrl+C landing inside an Events/Snapshot/reconcile store call classifies as ExitRuntime (`run.follow: context canceled`, exit 6) while the same interrupt in the timer select exits 0 — nondeterministic outcome for an intentional observation-only interrupt.
- **F3 (Low-Medium)** — text-mode diagnostics prints escalated `Status: degraded/failed` without `health.Diagnostics` or storage/quota bytes; the escalation cause is JSON-only.
- **F4 (Low)** — support export embeds process-local metrics that are structurally ≈0 for any fresh CLI process, unqualified, inviting "nothing was appended" misreads.

Defended non-issues include read-triggered reconcile (intended per TRD §18C), the ParseDuration swallow (unreachable — config.Load validates first), envelope status divergence, cancel idempotency/CAS, the two-layer payload trust boundary, list N+1 boundedness, and follow timer discipline.