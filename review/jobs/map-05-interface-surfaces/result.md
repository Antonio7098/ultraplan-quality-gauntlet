The existing `maps/map-05-interface-surfaces.md` is from an older run against a different frozen commit (`f0fcd0c…`) than my assignment (`f73c4dc…`). I need to produce a fresh independent map. Let me confirm the exact job prompt and verify the target repo state.Live tree is at `8eef70f`, one commit ahead of frozen `f73c4dc`. I must pin discovery to the frozen commit. Let me check the job artifact conventions from completed peers first.Conventions confirmed: peers pin a clone at the frozen commit, write the full map to `review/maps/map-05-interface-surfaces.md`, and return a summary (harness captures it). The frozen→live delta (`go.mod`/`go.sum` only) touches no interface code, but I'll still discover against a pinned clone.Pinned clone verified at `f73c4dc`, clean tree. Quick orientation:Orientation complete: single binary (`cmd/ultraplan`), `internal/app` hosts CLI dispatch + shared use cases, `internal/tui`, `internal/web`. Launching five bounded `review-worker` discoveries in parallel against the pinned clone.All five briefs returned. Now verifying the load-bearing claims directly at the pinned commit before composing the map.Core claims verified. Second verification batch:All load-bearing claims verified at `f73c4dc`. Quick peer-map cross-check for gap coverage:One last verification of the readOnly divergence fact before composing:All verified. Writing the map:Job `map-05-interface-surfaces` complete. Map written to `review/maps/map-05-interface-surfaces.md` (replacing the stale pre-bind map written against `f0fcd0c`). Discovery ran against a pinned clone verified at frozen `f73c4dc`; the live tree's only extra commit touches go.mod/go.sum, so all facts hold for both. Target and workspace untouched.

**Map summary — 8 candidate surfaces across 4 domains, 8 interface seams:**

| Domain | Surfaces | Risk |
|---|---|---|
| A · CLI shell | `cli-dispatch-exit-contract`; `run-cli-observation-control` | high, normal-high |
| B · Web console | `web-routing-projection-compat`; `web-operation-hub-sse`; `web-security-session-origin` | high ×3 |
| C · TUI | `tui-dashboard-navigation-projection`; `tui-operation-lifecycle-cancel` | normal-high ×2 |
| D · Cross-interface | `cross-interface-usecase-vocabulary` | critical-as-lens |

**Load-bearing structural facts (all mapper-verified at `f73c4dc`, ~20 spot-checks):**
- **Three durable-acceptance mechanisms over one `repository.Accept`**: CLI accepts with an *empty digest* (`durable_operations.go:55`) — its dedup alias is unreachable; web dedups on `sha256(session‖token)`; TUI on content-derived digests gated on `Confirmation != nil`. No test pins any formula.
- **Same action, different doors**: web composes use cases `readOnly:true`; TUI leaves refresh able to rewrite flow-state.json (its own help admits it). QA-cancel ownership precheck exists only on the CLI path. Cancellation reasons: CLI whitelist of 4 vs hard-coded `"user_requested"` in web/TUI. Study-cancel is a SIGINT lock signal that bypasses durable acceptance entirely (TUI issues it without PrepareOperation).
- **27-kind operation enum**: string literals unpinned anywhere; no TUI↔enum or CLI↔web value parity tests; `study-cancel` has no web issuer; CLI folds `--resume` into `execute-start`.
- **Security drift points**: `validCommandOrigin` docstring describes port tolerance its body doesn't implement; HTML `_csrf` uses plain `!=` while API CSRF is constant-time; session cookie has no Secure flag and no server-side expiry; sprint-create mutates outside the token flow.
- **Projection honesty gaps**: rune-unaware truncation in TUI; 512/1s/250ms replay constants triplicated unpinned; QA/runcontrol DTOs pass through raw with no frozen schema; artifact refs die on restart.

Reviewer priority for later phases: D1 divergence → B2 hub/drain → B3 security controls → C2 TUI lifecycle → A1 exit-code contract → B1/C1/A2 projection surfaces.