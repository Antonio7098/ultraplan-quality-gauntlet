I'm the quality-context-builder agent executing this job. Let me verify the frozen state and study the surface.Frozen commits verified. Now reading the primary surface files.All research complete. Writing the context pack.Context pack written: `review/surfaces/context-sprint-verify-transition.md` (139 lines, descriptive only, no defect claims).

Coverage summary:

- **Purpose/control flow**: `Service.Verify` (verify.go:39-95) as sole promotion boundary — lease once via `acquireMutationContext`, `requireCompleteExecute` incl. legacy `.run-state.json` summary fallback, `VerificationStatus` freshness recomputation with read-only expired-attempt reconcile, currentness-reuse vs. forced review rerun, diagnostic continuation ladder (`allowDiagnosticContinuation`, verify.go:74), smoke delegation; CLI wiring (parse/`--yes` gate/durable run-control op/envelope/exit-5-on-fail-blocked), Flow dry-run+post-execute delegation (flow.go:97-105, 178-182), web/TUI operation kinds, verification_phase.go adapter, freshness_policy.go disabled strict switches.
- **State/invariants/trust**: flow-state Review/Smoke blocks (digests/fingerprints/LastComplete/attempts/DiagnosticOverride) as gate authority; double decision-time revalidation (Verify + prepareSmokeStatic:177-209); diagnostics-cannot-become-canonical enforced by deriveAssessment override-blindness + DiagnosticOnly pre-commit return + roadmap/publication suppression; stale reviews never overridable.
- **Lifecycle/effects**: cancellation via durable operation context, typed conflict/interrupt semantics, single long-held lease, error propagation with intentionally discarded status-refresh errors on two legs.
- **Contracts/tests**: PRD/TRD/roadmap CURRENT-CONTRACT lines; 8 verify_test.go tests + smoke selection pins + app-level parse/help/envelope tests itemized; factual note that no test invokes `Service.Verify` directly.
- **10 explicit unknowns** for reviewers: fail-vs-blocked ladder asymmetry between Verify and the smoke gate; untested Verify control-flow branches; `NonInteractive` defined but unconsumed in-package (web relies on confirmation tokens); zero-valued Verification on missing flow state; lease liveness during multi-hour transitions; PID-reuse/unix-only expiry; targetIdentity reachability under disabled switches; hand-edited verdict handling; exit-code contract for errorless fail assessments; service-level Flow lacking a NonInteractive gate.