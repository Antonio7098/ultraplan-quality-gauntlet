Frozen commits verified. Now reading the primary path files.The sibling pack gives me the exact format. Gathering remaining evidence: prompts, applicability, durable CLI wiring, runtime factory.All evidence gathered. Writing the context pack.Context pack complete: `review/surfaces/context-study-task-execution.md` (196 lines), mirrored to `review/jobs/context-study-task-execution/{result.md,attempt-01-result.md}`.

**Coverage:** both entrypoints (`study run`/`study synthesize` CLI wiring through `executionService` + durable operation acceptance) and internal callers (run-loop with ResumeSession/OnSession/DeferPublication; run-all without); the shared `startRuntime` core — fingerprint composition per task kind, continuation gate (`ContinueFailures==0` + provider/model/workdir/fingerprint equality), one-shot poisoned-checkpoint fresh fallback with its nine suppressed categories, checkpoint-on-event emission; the agentwrap validation spec (required-file expectation, ≤2 same-session repairs, fresh fallback) and the lexical per-source/final validators; edit monitoring (before/after hash diff vs allowed output path, ~1s settle loop); clean-exit recovery when a validating report exists despite `runtime_exit`; scoped runtime store lifecycle and post-success store/session deletion; publication gating.

**Also pinned:** authoritative state split (report trees / `.ultraplan/runtime/opencode/<hash>/` stores / run-state TaskSession checkpoints), 12 implemented invariants, trust boundaries (LLM markdown → artifact + success classification via substring/rating/citation-regex checks only; opaque SessionIDs replayed as capability handles), cancellation/retry/restart semantics, 20 tests mapped to behaviors, sprint-10/TRD contracts, baseline green.

**Notable descriptive facts for reviewers (§16):** `agentMetadata` receives base config so Agent.Provider/Model ignore `--model` overrides (per-attempt fields stay accurate); edit monitoring runs and adds settle latency even on failed/cancelled runs and gives up after ~1s; markdown-analysis fingerprints cover document changes only via prompt.Text; `runtime_exit`+invalid-artifact maps to validation_failed which the run-loop never retries; CleanupSessionIDs populated regardless of actual deletion mode; no dedicated unit tests for the snapshot/warning helpers; plain CLI cannot resume sessions (run-loop only).