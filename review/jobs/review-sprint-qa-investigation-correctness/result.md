I'll start by loading the gauntlet skill and reading the context pack, then dig into the source.The target HEAD (`4841b74`) doesn't match the assigned frozen commit (`c6f01cf`). Let me check the relationship without touching the target repo.The assigned frozen commit (`c6f01cf`) is 5 commits ahead of the context pack's snapshot (`8eef70f`), including QA-touching commits ("Retry malformed QA investigator output", "Unify QA repair..."). I'll export the frozen tree read-only to /tmp for inspection and testing, leaving the target untouched.The context pack is materially stale vs. my assigned commit (~2,276 added lines across QA, including 6 new files). I'll review the actual frozen source. Starting with the core files in parallel.Now the persistence core:Persistence layer mapped. Now the map builder and synthesis:Now the prompt/check-execution layer and the new evidence subsystem:My temp extraction was removed (shared /tmp with other jobs). Re-extracting under a unique name:The new evidence subsystem raises several candidates. Now the wiring — durable operations, commands, use cases, locks:Tests confirm evaluator semantics are deliberate. Now checking the authoritative planning workspace contracts for the QA surface at the frozen workspace commit:Contract context established. Now verifying key hypotheses — first `targetIdentity` composition (does the implementation fingerprint include git index/status?), and `SaveFlowState` pointer preservation:`targetIdentity` confirmed content-based (no index state) while `qaGitIdentity` feeds volatile git index/status into persisted map bytes under an immutable CAS keyed by content-only IDs — a concrete conflict candidate. Now checking flow pointer publication and remaining wiring:`RunSmoke` takes its own lease — smoke-suite path defended. Now establishing the test baseline at the frozen commit in my isolated copy:Baseline green. Now writing targeted falsification tests in my isolated copy for the three leading candidates (never touching the target repo):All three repros confirmed. Continuing the sweep — checking remaining callers and a few last candidates:Zero-`Flow` publishes fail by design (backfill covers only pointers); production always passes a loaded flow. Fixing the repro to mirror production:All four candidates now have passing falsification repros. Final sweeps — exit-code constants, remaining tests, and full suite integrity:All verification complete. Checking artifact naming conventions, then writing up:The sibling summary mentions a gofmt exit-code probe — I need to verify this interaction myself since it determines whether the shipped check *blocks* shards when it detects drift:The pinned test uses a fake runner — the nonzero-exit path is untested. Final repro: investigator requests the shipped gofmt check on drifted code with the real DirectRunner:All five repros pass — and the format-check repro additionally shows blocked shards carry **no blocker and no attempt diagnostics** on this path. Full suite check, then writing deliverables:Target/workspace untouched (the `skills.go` modification pre-dated this review). Writing the findings report:Review complete. Deliverables written:

- `review/surfaces/review-sprint-qa-investigation-correctness.md` — 8 findings + 9 defended/non-issues
- `review/jobs/review-sprint-qa-investigation-correctness/result.md` — job summary

**Key results** (all at the assigned freeze `c6f01cf`; note the context pack was 5 commits stale and missing the entire v2 evidence subsystem):

| # | Finding | Sev |
|---|---------|-----|
| F1 | `DeriveQAAssessment(..., nil)` at qa.go:537 drops retained blockers + no coverage gate → blocked shards / focused runs publish `assessment=pass` + qa.md with uninvestigated coverage | High |
| F2 | `gofmt -d` exits 1 on drift (probed); check runner maps that to RuntimeUnavailable → requesting shard blocked, theories discarded, no diagnostics persisted | High |
| F3 | Plain `qa start` overwrites terminal shard records in the same content-addressed attempt dir — destroys prior theories irrecoverably | Med-High |
| F4 | Volatile git index/status hashes inside byte-CAS'd map bytes → permanent spurious Conflict after `git add`, with wrong recovery guidance | Med |
| F5 | FocusShard + proposed follow-ups ⇒ batch #2 InvalidState fails an otherwise-successful run | Med |
| F6–F8 | RecoverQA persists Stale on transient failures; follow-ups bypass lowered TotalShards budgets; evaluator digests bind to never-published pre-flip bytes | Low |

Every defect claim is backed by a passing falsification repro in `/tmp/opencode/rev-qa-sqi/internal/sprint/zz_repro_sqi_test.go` (baseline suites green, target/workspace repos untouched). Defended non-issues include the evaluator fail→pass majority flip (deliberate, tested), writer-fence fail-closure, pointer-last publication, and smoke-suite lease safety.