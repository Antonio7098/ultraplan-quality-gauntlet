# Surface Map — map-04-execution-paths (execution lifecycles)

Target: `/home/antonioborgerees/coding/ultraplan/ultraplan-go` @ `f0fcd0c2107a8e8d69e1283f9e8d5e2c6da94025` (frozen).
Planning context: `/home/antonioborgerees/coding/ultraplan/ultraplan-workspace` @ `ab12dc38059c9bf485f9aced9075bcd7d924cac5`.
Method: 8 parallel read-only `review-worker` discovery tasks (single-run, run-loop, flow chain, execute/resume, review/smoke/QA, runtime/process supervision, publication/worktrees, observation/web/TUI + workspace/config). Descriptive only; no findings asserted. All `file:symbol` refs below were verified by workers against the frozen tree.

Lifecycle lens applied to every surface: request entrypoint → prompt/input assembly → runtime/process invocation → cancellation/interruption → result validation → persistence ordering → observation.

---

## Domain grouping

| Domain | Surfaces |
|---|---|
| D1 Study workflows | S1 study-single-run, S2 study-runloop-resume |
| D2 Sprint planning | S3 sprint-flow-stage-chain |
| D3 Sprint execution & verification | S4 sprint-execute-resume, S5 review-smoke-gate |
| D4 Runtime/platform substrate | S6 runtime-process-supervision, S7 durable-operation-control, S8 publication-and-worktrees |
| D5 Observation & control | S9 observation-web-tui |
| D6 Foundation | S10 workspace-init-config-health |

---

## S1 — `study-single-run` (`study <s> run <dim> <src>`, `study <s> prompt ...`) — risk: normal

Lifecycle: `main` signal ctx (`cmd/ultraplan/main.go:18`) → `runStudyRun` (`internal/app/study_commands.go:802`) wraps itself as durable op (`beginDurableCLICommand`, OperationStudyStart) → `executionService` (:860) builds `NewOpenCode` → `controlledRuntimeFor` → `study.NewService(WithRuntime, WithPublisher)` → `Service.RunAnalysis` (`internal/study/run.go:19`): `BuildAnalysisPrompt` (applicability gate `SourceAppliesToDimension`, base.md override-or-builtin merge, directory-vs-markdown variants) → `startRuntime` (:108) sets WorkDir=study dir, per-owner scoped runtime store `ScopedRuntimeStorePath` (`platform/runtime/store.go:48`, owner hash of study+kind+dim+source), isolation policy deny `external_directory`, ValidationSpec with MaxAttempts=2 repair → blocking `StartRun`; **the agent writes the report file**; ultraplan validates post-hoc (`ValidateSourceReport`, `validation.go:13`; rating parse). Failure-but-valid-output is recovered to `completed` (run.go:74–86). Edit warnings via snapshot/settle loop (`edit_warnings.go`). Publication only when `git.stage_completion != off` (default off, config.go:190). Cancellation → status cancelled, ExitCancel=7.
State authorities: none in study run-state (single runs never touch `run-state.json` — grep-verified caller set); durable trace lives only in the runcontrol ledger; runtime store retained on failure.
Locking: **no study-level lock** for single runs (`AcquireRunLoopLock` called only from RunLoop/reconcile paths).
Observation: `study status` requires run-state (fails after only single runs); `summary` regenerates CSV from reports; `run list/show/follow` shows the durable ledger record.
Primary files: app/study_commands.go (run/prompt/executionService), study/{run.go,prompts.go,reports.go,resolve.go,validation.go,rating.go,edit_warnings.go,publication.go}.
Pinning tests: run_test.go (`TestRunAnalysisSuccessMapsRuntimeRequestValidatesAndDeletesSession`:137, continuation trio :54/:81/:118, failure matrix :176, recovery :209), study_run_commands_test.go (:61 success/skip/diagnostics incl. prompt non-leak asserts), study_prompt_commands_test.go (:115 no-runtime boundary).
Candidate risk hypotheses (for reviewers, not findings): concurrent duplicate single runs share one deterministic store owner hash; no mutual exclusion; pressure machinery absent on this path.

## S2 — `study-runloop-resume` (`run-all`, `run-loop [--reset|--continue|--force-unlock]`, `synthesize`, `runs summary`) — risk: high

Lifecycle: `runStudyRunLoop` (app/study_commands.go:197) confirms destructive reset, opens durable op, `service.RunLoop(durable.Context(), …)` (`internal/study/run_loop.go:23`). Graph built/persisted by `NewRunState`→`buildCurrentTaskGraph` (analysis tasks + synthesis nodes with dependency edges, applicability fingerprint sha256). Scheduler claims up to effective parallelism (default 3, ops cap 1..64), refills slots without sibling cleanup; dynamic throttle on memory pressure (15%/25% thresholds, memory_pressure.go) and disk admission pause/cap (disk_pressure.go). Per task: persist `running` transition BEFORE runtime call → session checkpoints persisted on every OnSession → agent writes artifact → validate → terminal transition persisted → history append (artifact → validate → state → history ordering). Dual authority: productstate DB row preferred when present; JSON mirror written only when complete (state.go:59–71). Atomic writes everywhere (temp+fsync+rename+dirsync); debounced coalesced persistence (250 ms min interval). Startup sequence: lock (O_EXCL + PID liveness + stale reclaim) → load-or-create → CleanupRuntimeStores(72h/2GiB) → ReconcileRunState → ResumeValidateRunState (reset active→pending; revalidate completed artifacts, demote invalid) → optional RestoreCompletedRunHistory (requires passing history record; never adopts stale files) → initial saves. `--reset` archives `run-state.json` to `.ultraplan/archive/run-state-<ts>.json` only. Retry tiers: runtime categories retryable (rate-limit +10 min, else +2 min); machinery errors (`firstErr`) stop scheduling while workers drain. Cancellation: unscheduled stay pending (pinned), claimed-but-unstarted self-cancel, in-flight get cancelled category; `CancelRunLoop` SIGINTs live cross-process owner; force-unlock removes lock. Synthesis: graph node gated on deps; terminal-non-complete deps fail synthesis (`synthesis.dependencies_failed`). Publication deferred to dedicated worker goroutine + terminal triple publish (publication.go:45–63). RunAll variant: worker pool, serial synthesis, bypasses lock AND run-state entirely.
Primary files: study/{run_loop.go,run_all.go,run_state.go,run_state_domain.go,state.go,state_database.go,run_history.go,run_history_summary.go,locks.go,cleanup_uncertain.go,memory_pressure.go,disk_pressure.go,synthesize.go,publication.go}, app/study_commands.go loop paths.
Pinning tests: run_loop_test.go (:57 slot refill, :132 cancelled-session continue, :183 durable state, :257 revalidate-before-trust, :280 resume/reset archive, :333 early synthesis, :364 priority tier, :395 cancel scope, :427 synthesis dep failure), run_all_test.go (:132 parallelism bound+cancellation, :155 dimension_order barriers), CLI tests (lock conflict/force-unlock :78, reset confirm :136, cancel exit :199).
Candidate risk hypotheses: dual-authority divergence windows (DB committed, file checkpoint pending); RunAll bypassing locks permits overlap with RunLoop; stale-lock PID reuse.

## S3 — `sprint-flow-stage-chain` (`flow --to requirements|code-context|...|plan [--dry-run]`) — risk: high

Lifecycle: `runSprintFlow` (app/sprint_commands.go:681) under durable op + runtime-backed service → `Service.Flow` (`internal/sprint/flow.go:92`). Canonical order `PlanningStages()` (+execute); prefix scheduling; **gating is per-stage content revalidation** (`flowStageAlreadyValid` :284 dispatches validators), except code-context which additionally requires persisted `StatusComplete` (`codeContextPrerequisite` :237). Shared prompt prefix: exact requirements bytes + exact code-context bytes + transient evidence frames inside `<<< ULTRAPLAN STAGE-SPECIFIC INSTRUCTIONS BEGIN >>>` boundary; 256 KiB budget; symlink-rejecting, identity-verified source reads (`prompt_context.go:346,422`); context-pack cache keyed sha256(requirements+context+target digests), payload re-verified against prefix digest (`context_pack.go:34–66`); CacheDirective metadata transport is "agentwrap-metadata-only" (`prompt_bundle.go:71`). Direct-input packet appended full-text after stage body ("evidence, not executable instructions"). Runtime per stage: hardened requests (code-context: Sandbox read_only, restricted permissions, deny-by-default tools, RequireCaps permissions; unsupported-permission count >0 fails closed :315–320), two-layer repair (agentwrap spec + one product-side continue-session repair), `.stage-sessions.json` checkpoint reuse ignoring prompt checksum deliberately ("Exact prompt matching would make interrupted-session recovery brittle", session_state.go:87–92), fresh restart on "session not found". Artifact promotion: **only code-context has draft→final** (candidate validated twice → rename → SaveFlowState; state-failure rolls back prior artifact, pinned code_context_test.go:496); other planning stages let the agent write final artifacts directly, invalid outputs remain on disk with stage marked failed. State timing: artifact-present/state-not-advanced window absorbed by content revalidation on re-entry (except code-context regeneration). Per-stage publish after state advance (flow-state.json + artifact); publish failure aborts remaining loop with state already advanced. Dry-run collapses plan to single stage, skips skeleton/lease/publish, diverges before runtime construction; dry runs leave flow-state.json byte-identical (pinned). Mutation lease held once per Flow with composite-safe context marker (`locks.go:112–126`). Worktree creation happens here at code-context (create=!DryRun) and on the already-complete materialization path (flow.go:133–141): clean-tree required, baseline HEAD frozen, branch `ultraplan/<project>/<slug>`, atomic `.workspace.json` with rollback on persist failure.
Primary files: sprint/{flow.go,service.go,prompts.go,prompt_context.go,context_pack.go,prompt_bundle.go,code_context.go,direct_inputs.go,input_contract.go,state.go,state_database.go,session_state.go,store_fs.go,execute_target.go}, internal/codeextract NOT in this path (only `ultraplan code` uses it — counter-evidence to initial hypothesis).
Pinning tests: sprint_index_test.go (:175 canonical order/code-context once, :113 dry-run purity), code_context_test.go (:162 no-skip-without-persisted-success, :201 request hardening, :496 rollback), sprint_test.go (:100 strict load/atomic write), sprint_workspace_test.go (:13 freeze, :43 dirty reject, :59 materialize-without-rerun), session_state_test.go lifecycle set, app/sprint_commands_test.go (:352 dry byte-identity, :464 configured runtime).

## S4 — `sprint-execute-resume` (`execute [--resume|--task T --defer R|--dry-run|--model]`) — risk: critical

Lifecycle: `runSprintExecute` (app/sprint_commands.go:329–368) durable op (confirmation class "RUNTIME + APPROVED TARGET MUTATION") → `Service.Execute` (`internal/sprint/execute.go:129`). Plan gates: `ValidatePlanContent` (required sections, reasoning citation, traceability, forbids literal git commit/push content); task extraction only top-level `- [ ]` titled `Task N:`; **content-derived IDs** `task-sha256(name+decisions+reqs+evidence)[:12]` (stable identity, dedupe findings); `[x]` allowed only on resume path; `[/]` requires inline deferral reason. Target: `resolveSprintTarget(create=false)` reuses validated `.workspace.json` (same SourceRoot, same git common dir, branch match; dirty worktree acceptable) else falls back to project-index path. Sequential queue, batch-session reuse: first turn full prompt (shared prefix + direct inputs + ordered queue + safety instructions "Do not run or request Git mutation…"), subsequent turns compact continuation with same SessionID (`execution_session_mode continue/fresh-fallback`); model precedence override → execute_model → plan_model → models.primary/default plus inline `<!-- model: -->` annotations. Per-task state machine pending→running→complete|deferred|failed|cancelled in `<sprint>/.run-state.json` (dual-backed by productstate kind `sprint_execute`; file mirrored only at all-terminal checkpoints). Strict persistence ordering: save `running` BEFORE runtime (failure aborts with zero runtime calls, pinned); mid-run checkpoint on new SessionID; failed checkpoint ⇒ task failed(`state-save-failed`) and queue abort. Completion requires appended runtime artifacts as Evidence or explicit `diagnostic-only` warning; success without evidence ⇒ failed(`missing-evidence`). Outcome priority order pinned: checkpointErr → invalid-deferral → agent-deferred → ctx-cancelled → runtime-failed → evidence-complete. Failed/cancelled hard-stop the queue; cancelled tasks remain resumable. Resume: `reconcileExecuteState` converts stale running→failed(`stale-running`), merges by ID; `validateResolvedResumeTasks` cross-checks plan markers vs records. Web-server boot reconciles dead-owner mutations (`ReconcileInterruptedMutation` flips running→failed(`recovery-interrupted`), expires attempts, consumes `.cleanup-uncertain.json` only if it changed something). Lease held for entire run; PID-liveness takeover; no heartbeat renewal of the file lock itself. Post-completion publication: implementation repo commit `All:true` FIRST, then workspace `execute.md+.run-state.json` (order pinned) — before any verification. `flow --to execute` always resumes. No production worktree prune/remove exists (by design per docs).
Primary files: sprint/{execute.go,execute_model.go,execute_plan.go,execute_state.go,execute_target.go,plan.go,session_state.go,locks.go,verification_lock.go,cleanup_uncertain.go,runtime_metrics.go,publication.go}.
Pinning tests: execute_state_test.go (:233 no-runtime-on-save-failure, :264 checkpoint failure diagnostic, :174 legacy never-resumable, :44 atomic write), execute_plan_test.go (:31 stable IDs, :83 deferral keeps ID), efficiency_improvements_test.go (:140 single-session continuations, :215 queue stop-after-failure), locks_test.go (:11 composite safety), verification_lock_test.go (:11 liveness), cleanup_uncertain_test.go (:12), sprint_workspace reuse pins.
Candidate risk hypotheses: long-held un-renewed file lock vs PID reuse; plan-content drift during multi-task executes (PlanFingerprint stored but no equality gate found in reconcile — mechanism observation); All:true target commit scope.

## S5 — `review-smoke-gate` (`verify`, `review`, `smoke`, `qa run/resume/map/status/recover/cancel`, `validate execute`) — risk: high

Lifecycle: `Verify` (`internal/sprint/verify.go:39`) is "the sole review-to-smoke transition": `requireCompleteExecute` (every task complete-or-deferred + non-empty execute.md) → Review unless current evidence fresh+completed+not restarting → smoke gated by verdict. Diagnostic continuation past failing review requires To==smoke && ForceReview && OverrideConfirmed && rationale (verify.go:74–77); derived assessment precedence ensures "diagnostic smoke cannot change this result" (pinned verify_test.go:209). Review: fingerprinted manifest over governed artifacts + area reasoning + target `.identity` revision + changed paths (containment-checked); frozen reviewer snapshot under `.ultra/cache/review/...` with `.complete` marker; bounded reviewer fan-out (default 3, cap 16) with sandboxed read-only deny-by-default sessions; incremental durability (resume checkpoints in flow-state.json, per-result saves, heartbeat+PID); focused rerun requires identical InputFingerprint + retained coverage; fingerprint-change rebases validated results; deterministic verdict (diagnostics⇒blocked, blocker/high⇒fail, any⇒pass_with_findings, else pass); artifact promoted only when fully validated (atomic temp+fsync+rename); completed-but-fail still publishes then errors. Smoke: static preflight enforces exactly-one cataloged harness, contained roots, then the review gate (missing/stale/blocked/invalid reasons; stale cannot be overridden); scope selection defaults to narrowest complete sprint mapping, partial/explicit scopes become DiagnosticOnly; **diagnostic results are never canonical** (return before commitSmoke: no artifact, no state, no roadmap); commit validates content (secret-pattern bans) then rewrites state wholesale; canonical pass triggers roadmap delivery reconciliation then publish (harness authoring changes published separately before workspace smoke.md). QA: separate shard machine (mapped→queued→running→synthesizing→completed; blocked/cancelled/interrupted/stale side-states) behind writer tokens + heartbeat fences (`ErrStaleFence`), pointer-last publication into private 0600 state.json then bounded QAFlowSummary into flow-state.json, hard bytes budgets, strict DisallowUnknownFields decoding, RecoverQA without runtime. Freshness: snapshot-freshness switches intentionally false (freshness_policy.go:14); effective freshness = digest + content validation + LastComplete presence. Crash windows mapped structurally: review artifact-before-digest (reported not-fresh, safe direction), smoke artifact/state mismatch flagged `Reconciliation=true`. Attempt expiry derives truth without mutating on read paths.
Primary files: sprint/{verify.go,review.go,review_runtime_validation.go,smoke.go,smoke_author.go,smoke_protocol.go,smoke_types.go,qa.go,qa_map.go,qa_state.go,qa_synthesis.go,qa_prompt.go,qa_types.go,verification_phase.go,verification_lock.go,freshness_policy.go}, app qa_errors.go exit mapping (qa.persistence_failure etc.).
Pinning tests: verify_test.go (:30 precedence matrix, :123/:155 expired/dead attempt recovery without writes, :180 freshness+focused cost, :224 typed conflict), review_test.go (:252 resume-in-fresh-session, :294 rebase after fingerprint change, :320 restart discards, :601 atomic preserve, :657 blocked can't pass, :189/:232 fingerprint stability), smoke_test.go (:192 complete-mapping requirement, :301 malformed preserves prior, :287 authoring allowlist), qa_* suite (pointer-last publish + modes, strict decode, panic containment, retry caps), app parity/help pins (TestParseSprintVerifyAndFlowSmokeParity, TestVerifyHelpExplainsGateAndRecovery).

## S6 — `runtime-process-supervision` (internal/platform/runtime + internal/platform/process) — risk: critical (substrate)

Adapter stack (`NewOpenCode`, opencode.go:31–127): ObservingRuntime{ValidatingRuntime{PolicyRunner{requestVariantRuntime{*opencode.Runtime}}}} + PersistencePolicy(raw payloads omitted) + missingSessionPolicy→BasicPolicy(MaxAttempts=retries+1, exp backoff 1s→30s, rate-limit retries, backup-model fallback). agentwrap boundary: prompt via stdin; `exec.CommandContext` spawn of `opencode run --format json`; env `OPENCODE_DB=<scoped path>`; tool/permission policy enforced natively via OPENCODE_CONFIG_CONTENT (translatePermissions incl. PathRules→external_directory); metadata carries cohort-scoped prompt-cache keys. Events: per-run goroutine drains run.Events() → mapEvent (payload sanitize ≤64 fields/≤8KiB/depth≤3, raw never retained) → ring buffer retainedRuntimeEventLimit=200 with drop counter + warning; 1 s handshake give-up ships result without events; terminal output bounded 96 KiB; channel backpressure reaches stdout scanner (native chan 32). Non-agent DirectRunner (smoke/QA subprocesses): unix pgid TERM→grace→KILL ("final group kill closes the race"); other platforms single Kill with abandoned-wait CleanupComplete=false; output limits 4 MiB/1 MiB. Cancellation: ctx done → run.Cancel → CommandContext kill + 2 s bounded cleanup; process-group SIGTERM→SIGKILL (linux Setpgid+Pdeathsig); ≤5 s local wait else synthesized cancelled result; DeadlineExceeded→timeout category (loop treats as retryable +2 min). Timeout enforcement inside agentwrap (`context.WithTimeout(ctx, req.Timeout)`), default 30m from config. SQLite runtime stores: `store.json` {active|retained|cleanup_pending} beside each `opencode.db`, prepared with PID at StartRun, retained on EVERY exit path; CleanupRuntimeStores(scope,72h,2GiB,aggressive) spares active/live owners, stale-active (>30 min dead PID) flipped to retained kept resumable ("A dead owner means an interrupted task, not disposable data"); managed-root path validation rejects escapes (pinned). OpenCode log pruning oldest-first, 48h/128 MiB, 10-min recent-touch skip. Delete hooks: SQL event_sequence purge + session delete + WAL checkpoint/VACUUM sandwich. Health: `health` command + in-spawn preflight when RequireHealth set; capability gaps downgrade to Unsupported→ExitRuntime. Concurrency: per-owner DBs isolate contention; package-global cleanup mutex serializes deletes; shared RateTableStore per adapter; runcontrol DB is the shared WAL store (_busy_timeout 5000, CAS updates, fenced attempts).
Primary files: platform/runtime/{opencode.go,runtime.go,agentwrap.go,events.go,store.go,health.go,models.go,policy.go,cache cohort metadata}, platform/process/{process.go,process_unix.go,process_other.go}.
Pinning tests: runtime_test.go (:258 ring buffer/drop warning, :481 ctx-cancel→Cancel+"cancelled", redaction/bounding set), opencode_test.go (:20 missing-session stop, :79 SQL escaping), store_test.go (lifecycle, interrupted-store preservation, aggressive-spares-live), process_test.go (:32 descendant cleanup verified via /proc zombie check), opencode_maintenance_test.go, cache_test.go (cohort scoping).

## S7 — `durable-operation-control` (internal/runcontrol + app wiring) — risk: high (backbone)

One SQLite ledger per workspace (`.ultraplan/run-control.db`, shared physically with productstate): tables runs (lifecycle enum accepted→running→terminal incl. cleanup_uncertain/persistence_degraded; CHECK tying outcome↔finished_at), attempts (fenced ownership: ordinal, fencing_generation monotone, host_digest/boot_id/pid/birth_token), events (append-only, immutability trigger RAISE(ABORT)), operation_aliases (idempotency digest→run), reconciliation_log. Writers: Accept (quota-checked) → Claim (single-owner CAS, fencing=MAX+1) → Heartbeat (fenced lease renewal; hard quota forces stop) → Append → RequestCancellation/AcknowledgeCancellation (event + CAS) → ProposeTerminal. Reconciler: unclaimed-after-grace→interrupted; expired leases probed via NativeProcessProbe birth identity→stalled/interrupted/cleanup_uncertain decisions. Retention: FullHistory 7d/Tombstone 30d/HardQuota 512 MiB; per-run compaction keeps ≤4096 deleting only progress/message/omission; compact full→compacted→tombstone→delete. IDs random base32 carrying no ownership info. App wiring: `controlledRuntime.StartRun` (app/run_control.go:159) Accept→Claim→Fence every runtime call, injects run_control_run_id, persists sanitized/coalesced events (250 ms window, sensitive-key denylist, omission records), 1 s ticker polls cancellation/heartbeat(15 s lease)/reconcile(10 s); **any persistence error cancels the live run and proposes TerminalPersistenceLost**. `beginDurableCLICommand` (durable_operations.go:49) gives CLI commands the same acceptance + supervisor goroutine; inventory test scans sources so every runtime-backed CLI entry must use it. FinishOperation maps deadline/cancel/error→terminal outcomes. External surface: `run list/show/follow/cancel/diagnostics` (+support export).
Primary files: internal/runcontrol/{sqlite.go,lifecycle.go,model.go,id.go,process*.go,retention.go,metrics.go,local_log.go,sanitize.go,migration.go}, app/{run_control.go,durable_operations.go,operation_runner.go(shared runner),run_commands.go}.
Pinning tests: runcontrol sqlite/lifecycle/migration/retention suites, benchmark_test; app durable_operations_test.go (:23 execute acceptance), run_control_inventory_test.go (:25 source-scan inventory).

## S8 — `publication-and-worktrees` (gitpublish + stage publishers + sprint worktree) — risk: high (destructive potential; opt-in)

Trigger: `stagePublisher(cfg)` returns nil unless git.stage_completion ∈ {commit, commit-and-push} (default off). Publishers wired into all sprint/study services. Mechanics (publisher.go): repo root via rev-parse from req.Root; detached HEAD refused; temporary index (GIT_INDEX_FILE) so user's staged index survives (pinned); read-tree parent → add -A -- paths → write-tree; tree-equality no-op idempotency; **CAS branch update** `update-ref <ref> <commit> <parent>` ("branch changed while committing"); real-index reconcile failure keeps commit + error; message trailer UltraPlan-Publication; push only in commit-and-push with upstream resolution, `--porcelain`, no force flags anywhere, GIT_TERMINAL_PROMPT=0, PushTimeout default 2m. Lock: flock `<git-common-dir>/ultraplan-publish.lock` (lock_unix); **lock_other.go has NO mutual exclusion on non-linux/darwin platforms** (open/create + return). Stage payloads: planning {artifact, flow-state.json}; code-context adds .workspace.json; execute = implementation repo All:true THEN workspace {execute.md,.run-state.json} (order pinned); review {review.md, flow-state.json}; smoke = harness authoring changes then {smoke.md, flow-state.json} + roadmap.md only on canonical pass. Study: report or summary or run-loop state triple; loop serializes publication through one worker goroutine; durable state never depends on publication success. Worktrees (S3 detail): creation only at code-context paths; validateSprintWorkspace checks SourceRoot/common-dir/branch but NOT Baseline-vs-HEAD nor dirtiness; no production cleanup/prune; docs explicitly disclaim automatic removal.
Primary files: platform/gitpublish/{publisher.go,lock_unix.go,lock_other.go}, app/git_publication.go, sprint/publication.go, study/publication.go, sprint/execute_target.go.
Pinning tests: publisher_test.go (:13 owned-paths-only + staged preservation, :47 push retry no dup commit), publication_test.go both packages (:26 exact paths, :55 execute ordering, :42 failed-stage-never-published), config_test.go (:156 git defaults/precedence/bounds), sprint_workspace pins.
Candidate risk hypotheses: All:true commits entire dirty target repo state; non-unix lock placeholder; lingering ultralan-index-* temp files after SIGKILL; Baseline drift undetected on reuse.

## S9 — `observation-web-tui` (`serve`, web dashboard, TUI, status JSON, productstate reads) — risk: high

Serve: loopback-only enforced twice (flag parse + resolved listener re-check); server caps constants (MaxInFlight 32, WriteTimeout 30 s with SSE-managed deadlines); startup reconciliation hook before serving; shutdown drains hub, cancels owned operations, on deadline persists `.cleanup-uncertain.json` (reason server_shutdown) BEFORE projecting terminal cleanup_uncertain (order pinned). Security: anonymous signed session cookie (HMAC, HttpOnly, SameSite=Strict), host pinning 403, Origin ladder (mutations require session+origin+CSRF; fetch-metadata port-strip tolerance), body/target/framing hygiene, strict CSP/no-store/DENY headers, server-generated X-Request-ID, concurrency semaphore, single-use preparation/confirm tokens (TTL 2 m, capacity 128, replay/stale detection), confirmation fingerprints sha256(canonical JSON + governed-input file hashes) recomputed pre-execution ("ExpectedFingerprint is server-issued authority"). SSE: transient hub (ring 256/op, slow-subscriber drop, gap detection→recovery_required+snapshot, whitelisted event names, 30 m lifetime cap) + durable poll-over-journal follow with cursor validation (cursor_ahead/replay_gap 409 with retention_or_compaction guidance). Operations: 26 kinds; two-phase prepare/start; mutation-class warnings pinned per kind; single `sharedOperationRunner` drives sprint/study services identically for CLI/TUI/browser; cancellations propagate runcontrol RequestCancellation→CAS→owner ticker ack→ctx cancel; TUI blocks quit with active durable work. Status JSON envelope schema_version 1 with RedactValue applied to lock/task fields. productstate: same SQLite file as runcontrol (WAL/FULL/immediate/busy_timeout, 0600, symlink checks); content-addressed header+items projections (kinds study_run, sprint_flow, sprint_execute); migration via `storage migrate` skipping already-imported; **read precedence DB-if-present, write mirrors to JSON files only at checkpoints** — mid-run file observers lag DB readers by design; plain loads elsewhere mean web status reads are whatever Load* returns at that instant.
Primary files: internal/web/{server.go,routes.go,security.go,operations.go,operation_handlers.go,run_handlers.go,timeline_handlers.go,qa_handlers.go}, internal/tui/*, app/{serve_commands.go,status_json.go,json_output.go,web_usecases.go,operations.go,operation_runner.go}, internal/productstate/store.go.
Pinning tests: api_compatibility_test (frozen route/schema matrix), operations_contract_test (kind table + SSE frame regexes cross-checked against embedded JS), security_test (full origin/CSP/framing matrix), operations_test (cleanup-uncertain ordering, capacity, replay bounds), routes/run/timeline/qa handler tests, tui suites, serve_commands_test (loopback ordering, "guarded"/"bounded SSE" help contract).

## S10 — `workspace-init-config-health` (`init-workspace`, `defaults install`, `skills materialise`, `config show`, `health`) — risk: normal

Init/materialise: create-if-absent only; traversal guard `ResolveInside` (rel-based escape rejection) on every planned path; **no EvalSymlinks anywhere in internal/workspace** (normalization is Abs+Clean only); defaults install compares against embedded bytes — identical no-op, differs+force overwrite, differs-without-force interactive confirm listing paths; skills materialise same regime, 11 skills under `.agents/skills/<name>/` with manual-only front-matter + `allow_implicit_invocation: false`; code-context skill is the narrow delegation exception invoking exactly `ultraplan sprint "$PROJECT" "$SPRINT" flow --to code-context`. Study init adds its own double containment (inside studyDir AND workspace) and clone support (`git clone --depth 1`, sequential, partial failures aggregated with credential-redacted output). Workspace discovery precedence inherited by all commands: `--workspace` flag → `$ULTRAPLAN_WORKSPACE` → upward walk to `ultraplan.yml` marker. Config: Defaults ← ultraplan.yml ← ULTRAPLAN_* env ← CLI(logging only), provenance tracked per field; hand-rolled YAML subset parser rejecting unknown fields; validation bounds (parallel>0, timeout positive, smoke maxima, git mode enum, remote charset no leading `-`, quota ≥64 MiB, health/capability whitelists); RedactValue constant `[REDACTED]` by key-marker matching applied across config show/health/status/progress/errors/logs. Health: ordered checks workspace.discovery→structure→config.validation→filesystem.read→environment.overrides→runtime.* (fresh NewOpenCode + adapter.Health with required checks/caps); exit mapping fail-runtime→6, config→3, structure→5, discovery→4.
Primary files: internal/workspace/{discovery.go,paths.go,init.go,defaults.go,skills.go,validation.go}, internal/app/{workspace_commands.go,defaults_commands.go,skills_commands.go,config_commands.go,health_commands.go}, internal/study/init.go+init_clone.go, platform/config/{config.go,redaction.go,qa.go}.
Pinning tests: workspace_test.go (:10 precedence, :59 init/validate, :154/:174 embedded defaults), skills_test.go (:10, :110 unknown selection), app_test.go (:104,:145,:212 redaction), skills_commands_test.go (:34 customized preserved unless confirmed), study init clone/safety pins, config_test.go full set, health_commands_test.go (:61 runtime exit, :75 JSON+redaction).

---

## Seams between surfaces

- **SE-1 shared-prompt-prefix contract** (S3→S4,S5,S6): byte-stable requirements+code-context prefix, boundary marker, 256 KiB budget, context-pack cache keyed by digests, cache-directive metadata advisory only. Correctness depends on digest discipline and read-identity verification.
- **SE-2 worktree handoff**: `.workspace.json` created in S3 (code-context), consumed by S4 execute (WorkDir), S5 review/smoke/prompt contexts, S8 publication Root. Reuse validation checks SourceRoot/common-dir/branch but not Baseline/dirtiness.
- **SE-3 flow→execute delegation**: `StageExecute` calls `Execute(Resume:true)`; skip gate uses `ExecuteComplete`; execute excluded from per-stage publish logic.
- **SE-4 execute→verify→review/smoke gate**: requireCompleteExecute; Verify sole transition; freshness computed from digest+validation+LastComplete with snapshot switches off.
- **SE-5 controlledRuntime wrap** (all agent surfaces↔S7): every StartRun Accept/Claim/fenced; events persisted with coalescing/redaction; persistence loss cancels live runs; CLI inventory test enforces adoption.
- **SE-6 scoped runtime-store ownership** (S2,S4,S5↔S6 maintenance): owner-string hashes determine DB paths; CleanupRuntimeStores invoked at loop start, disk-pressure pauses, and before each sprint runtime call; delete hooks purge sessions post-success.
- **SE-7 dual product-state authority** (S2,S3,S4↔S9): productstate DB rows authoritative when present; JSON mirrors written only at checkpoints; readers pick one source per load; migration via storage command.
- **SE-8 composite mutation lease** (S3,S4,S5): in-process sync.Map + O_EXCL file lock + PID liveness + context marker for nested stages; ErrVerificationConflict typed across surfaces; reconcile sweep at web boot.
- **SE-9 cancellation channels**: OS signals→signal.NotifyContext; external/web→runcontrol RequestCancellation CAS→owner tickers; serve shutdown→hub drain→cleanup-uncertain marker; three paths converge on ctx cancel inside services.
- **SE-10 publication gating**: nil publisher (mode off) short-circuits everything; stage-terminal states gate each publish call; durable state writes never depend on publication success.
- **SE-11 foundation feeds**: workspace discovery + validated Effective config supply every surface (parallelism, timeouts, retries, models per stage, sandbox/permissions, agentwrap executable, git mode, QA concurrency, smoke limits).
- **SE-12 single workflow core**: sharedOperationRunner + durableOperationManager used verbatim by CLI, TUI, and web; surface layers own presentation/confirmation only.

## Cross-cutting observations handed to later stages (hypotheses only, not findings)

1. Single `study run` has no lock and a deterministic store-owner hash → concurrent duplicate runs collide on one opencode.db (S1).
2. `RunAll` bypasses both the run-loop lock and run-state authority (S2).
3. Dual-authority windows: DB committed / JSON checkpoint pending (and legacy file-only workspaces) — observer divergence semantics (S2/S3/S4/S9).
4. Long-held sprint file lock has no heartbeat renewal; staleness resolved only by next acquirer's PID probe (S4/S5).
5. PlanFingerprint stored but no equality gate found in execute reconcile; per-task content-derived IDs shift if plan text changes (S4).
6. Freshness-policy snapshot switches disabled; effective freshness rests on digest+content+LastComplete (S5).
7. `ValidateExecuteWorkdir` appears production-unused (definition+test only); runtime containment currently prompt-enforced (S4).
8. lock_other.go provides no exclusion on non-linux/darwin (S8); process_other.go abandons wait after grace with CleanupComplete=false (S6).
9. Worktree reuse does not compare Baseline to HEAD; no production prune path (S3/S8).
10. Execute publication commits implementation repo with All:true — scope depends on operator cleanliness at that moment (S4/S8).
