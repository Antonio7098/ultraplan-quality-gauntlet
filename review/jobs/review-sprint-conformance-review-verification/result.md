Context pack read. Note it references a different frozen commit (8eef70f) than my assignment (c6f01cf), so I must verify everything against actual source. Let me survey the code.Both primary files read. Now I need supporting sources to confirm/refute hypotheses: the runtime adapter (event payloads, session semantics), review_patch, tests, and helpers.runtime.go reveals key mechanics: `promoteObservablePayloadFields` lifts nested tool-output strings to payload top level, and terminal output falls back to "last text-bearing event". Now the opencode adapter and agentwrap contract:agentwrap contract confirmed: validation judges each attempt's own RunResult; up to 2 repairs; events from all attempts funnel into one stream. Now the opencode adapter internals — TerminalOutput derivation and event payload shapes:TerminalOutput accumulates only from EventMessage/EventFinalResult kinds. Now the critical question: do tool outputs project as those kinds (and with which payload keys)?Projector confirmed: `tool_use` events carry deep-nested outputs, and ultraplan's `promoteObservablePayloadFields` lifts them to payload top-level where extraction looks first. Let me now verify how the sprint layer consumes events (kind-filtering?) plus service glue:`deleteCompletedSession` routes through the adapter's global-DB delete closure while review runs use scoped runtime stores. Let me verify how `MetadataDatabasePath` is consumed by the opencode runtime:Confirmed: scoped stores are selected via `OPENCODE_DB` env from request metadata, but the adapter's session-delete closure targets the default global DB. Checking `runtimeRequest` and store lifecycle to nail this down:Store lifecycle confirms the mismatch. Now the tests — assessing whether they'd catch these defects and where fake-only confidence lives:Tests analyzed — notable gaps: no focus-mode test, no tool-shaped event extraction test, no production-agentwrap-path test, no e2e fail-verdict test. Now checking remaining glue: exit mapping, verify interplay, helpers:Protocol read. Key divergences to weigh: implementation maps missing/invalid reviewer evidence to `blocked` (protocol §5/Stop Conditions say `fail`), and cross-coverage duplicate finding IDs hard-block with no self-healing path. Checking downstream treatment before judging:QA-map analysis done. Now let me empirically verify the extraction-provenance hypothesis (Finding #1) with a repro in a scratch copy — leaving both frozen repos untouched:Repro confirms the injection converts a would-be `blocked` into silent `pass`. Now checking whether direct-input embedding caps content sizes (diff-blowup hypothesis):Direct-input truncation is explicitly documented as provider-owned (defended). All hypotheses resolved. Verifying the frozen repos are untouched, then compiling findings:Unexpected modification in the target repo — I must inspect whether it's mine:The `skills.go` modification predates my session's writes (I only read from this repo; all my writes went to `/tmp/opencode`) — it's unrelated concurrent work on a file outside my review surface. Noting it and moving on. Checking artifact conventions for this job:Writing up the final result. First confirming the sprint suite is green in the scratch copy (so findings aren't test-confounded):Review complete. Result written to `review/jobs/review-sprint-conformance-review-verification/result.md`. Both frozen repos untouched by me (note: target tree carries a pre-existing unrelated uncommitted edit to `internal/workspace/skills.go` from concurrent work; HEAD intact at c6f01cf).

## Findings summary

**F1 — high/high (reproduced):** Result attribution ignores event provenance. A crafted clean-pass JSON embedded in any file a reviewer reads (changed target paths are mandatory reading) is accepted as the reviewer's verdict whenever the model's own output lacks a parseable object — proven e2e in a scratch copy: identical prose-only reviewer run yields `blocked`, adding one tool-result-shaped event carrying the injected object yields silent `pass` with zero diagnostics and a published artifact. Root chain: `promoteObservablePayloadFields` lifts nested tool outputs to payload top level (runtime.go:675), and both the validator fallback and reverse event scan are kind-blind (review.go:1652-1686, review_runtime_validation.go:63-67).

**F2 — medium/high:** Post-success session deletion targets the global OpenCode DB while review sessions live in per-coverage scoped stores selected via `OPENCODE_DB`; cleanup silently no-ops and transcripts persist until the 72h/2GiB sweep. Execute uses the working whole-store delete path; review deviates despite having `RuntimeStorePath` in hand.

**F3 — medium:** Cross-coverage duplicate finding IDs deterministically deadlock the gate: resume replays both validated checkpoints forever with zero runtime calls, focus is unavailable (no `LastComplete` on blocked runs), only destructive `--restart` escapes. No per-coverage ID namespacing exists in prompt or validation.

**F4 — medium-low:** Protocol mandates `fail` ("remain failures", synthesized review where possible) for invalid/missing mandatory reviewer evidence; implementation emits verdict `blocked` with no artifact and retry-framed downstream guidance ("Satisfy the review prerequisite and rerun").

**F5/F6 — low:** Canonical review.md sorts findings alphabetically by severity string (info renders above medium); `flow --to review --focus-review <id>` is silently ignored when evidence is fresh.

Defended/non-issues include: snapshot marker trust (lease-serialized, fingerprint-keyed), focus-mode retained-result trust, swept-store resume resolving fail-closed (closes pack unknown #1), documented unbounded input embedding, PID-handoff heartbeat semantics, and the dead `ProvisionalVerdict` field.