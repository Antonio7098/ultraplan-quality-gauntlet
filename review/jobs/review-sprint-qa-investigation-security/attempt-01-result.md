Context pack read — note it was built against an older commit (`8eef70f`), while my frozen target is `c6f01cf` ("Unify QA repair and streamline review context"). I need to review the actual code at HEAD and watch for drift between pack and reality.Major drift since the pack: commit `c6f01cf` added qa_investigation.go, qa_evidence.go, qa_adjudication.go, qa_report.go, qa_evaluator.go, qa_runtime_validation.go and rewrote large parts of qa.go/qa_state.go/qa_types.go. Reading all primary sources now.Now the wiring and trust-critical helpers:Baseline green. I've spotted a high-value candidate: immutable (CAS) evidence artifacts whose bytes embed wall-clock timestamps under content-derived IDs — re-publication over the same attempt should Conflict. Let me verify concretely and check counter-evidence.Repro confirms the immutable-CAS conflict. Now checking the new investigation/adjudication tests and the frozen workspace contracts for evidence-producing QA semantics (`--shard` focus, qa.md, promotion authority):HEAD appears to have moved — verifying immediately:HEAD advanced mid-review (new commit `4841b74` appeared on top of frozen `c6f01cf` — not created by me). Pinning analysis to the frozen commit and checking drift:Drift confirmed: `4841b74` (post-freeze) only touches the follow-up synthesis loop; all evidence-publication code I reviewed is identical at `c6f01cf`. Continuing counter-evidence hunt on the immutable-CAS finding — checking the expanded state tests for any pinned republication semantics:No counter-evidence — the evidence-publication test uses fixed timestamps and never republishes. Now closing remaining lens threads: config lower-only enforcement, and the runtime request seam (`runtimeRequest`, `splitProviderModel`):Tests pin the evaluator majority rule as intentional (`TestQAFailedShardRequiresThreeFreshCompleteEvaluators`) — noting that for calibrated confidence. Final seam checks:All threads resolved. Checking the gauntlet's artifact conventions before writing up:Citations verified against the frozen commit. Writing the deliverable:Review complete. Deliverable: `review/jobs/review-sprint-qa-investigation-security/result.md` (both target and workspace repos untouched; the pre-existing `skills.go` edit and mid-review HEAD advance to `4841b74` were not mine — analysis pinned to frozen `c6f01cf`).

**4 findings:**

1. **High/high** — Immutable-CAS evidence artifacts (`plans/`, `adjudication.json`, `assessment.json`) carry wall-clock/session-varying bytes under content-derived IDs that exclude those fields. Any second evidence publication into an existing attempt dir — plain rerun, resume after a failed final publish (incl. transient heartbeat failure), focused-then-full — hits `Conflict`, durably marks the attempt **blocked**, and exits 5 "validation". Reproduced with a targeted test at `c6f01cf`; rollback snapshots only state.json/qa.md/flow-state, so poisoned files are never cleaned.

2. **Medium-high** — `qa run --shard X` executes 1 of N shards yet publishes canonical `AssessmentPass`, `qa.md`, and issue pointers; neither admission (`MapComplete` is structural only) nor `DeriveQAAssessment` (blockers hardcoded nil) gates on execution coverage — contradicting the documented diagnostic rule for focused review/smoke. Composes with finding 1 so coverage can never be completed afterward.

3. **Medium/medium** — Three blind evaluator LLM sessions (digests-only packet, lenient decode, no repair loop) can flip a deterministic failing check to accepted passing evidence on a 2-of-3 vote, suppressing issues and enabling false-clean assessments. Tests pin this as intentional; reported as a soundness concern.

4. **Low/medium** — Publish rollback restores flow-state file bytes but not the DB mirror in DB-authoritative mode → pointer/digest divergence until recover.

Nine defended non-issues recorded (fence fallback reachability, SaveRecoveredState fencing, symlink/hardlink escape chains, bwrap isolation, output trust boundary, prompt injection, lower-only config, smoke-branch lease, PrepareReview purity).