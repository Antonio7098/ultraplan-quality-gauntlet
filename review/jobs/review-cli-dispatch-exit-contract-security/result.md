Context pack read. Target HEAD matches frozen `5d54525`, so I'll work directly on the tree. Starting with the core dispatch files.Now the sprint command file — the substring-classification ladders are the highest-value security target.Now the rest of sprint_commands.go, plus the durable operations helper and usecases (displaySafe).Dispatch core mapped. Now gathering evidence on the substring-classification sinks: what error text actually flows into those ladders, plus RefError formats and the flow runtime-failure test.RefError embeds `%q`-quoted refs — "runtime" substrings survive quoting. Now tracing whether `Flow` really returns raw RefErrors into the ladder, and how stage failures propagate.RefError flows raw out of `resolveSprintInputs*` into the ladder. Building the frozen binary to prove the exit-class flip end-to-end without touching the tree.Flip reproduced — and it's worse than expected: the *candidates list* from other workspace entries poisons unrelated lookups. Confirming variants and checking how stage failures propagate (inverse direction: real runtime failures lacking the substring).Stage runtime failures return raw provider errors with no findings — classification now hinges entirely on the message text. Checking what those actually say.agentwrap classifies errors by typed category *explicitly* to avoid string matching — but UltraPlan's CLI ladders re-introduce string matching downstream. Checking the intermediate wrappers to see what text reaches the ladder.The error reaching the CLI is a redacted `*agentwrap.SDKError` whose `Error()` renders `"op: category: detail"`. Categories like `authentication`, `rate_limit`, `provider_unavailable`, `model_unavailable` contain no "runtime" substring. Checking how tests simulate runtime failures (what message they use):No app test drives the substring branches with a genuine runtime-shaped error — the only pinned ExitRuntime comes from a classed persistence error returned before the ladder. Now the other admission surfaces: serve listen validation and code output handling.serve listen validation is tight (numeric parse, loopback, no zones, port bounds). Now the remaining command files.smoke.environment is name-only (validated `[A-Z_0-9]`, no values) — nothing to leak there. Continuing with health/storage/skills/study surfaces.Two potential sinks here: ANSI passthrough, and possibly git option injection if clone URLs aren't scheme-validated. Checking how clone URLs are validated:No scheme/format validation on clone URLs — they go straight into `exec.Command("git", "clone", "--depth", "1", url, dest)`. Testing whether a leading-dash "URL" gives git option injection (local command execution):Git option injection confirmed at the primitive level (`PWNED` created by the injected upload-pack). Now proving it end-to-end through the actual binary via `study init`:Checking whether the injected upload-pack fires even when the repo argument doesn't exist (as in the real flow):The upload-pack route needs an existing repo argument. But there's a cleaner one: the `ext::` pseudo-protocol runs a local shell command as the "remote side" — testing whether git 2.x allows it for non-root by default:The option-injection variant can't reach execution under the current argv shape (repo argument is always a nonexistent path, so git aborts before invoking transports) — that stays a hardening note, not a claimed RCE. But remote-controlled git output reaching stderr unescaped stands. Now checking tui_commands and the documented contract for cross-referencing:Provider errors pass through unwrapped (`run_control.go:342`). Closing the last coverage gaps: workspace_commands.go and the dispatch-level tests as evidence.Baseline green. Checking the gauntlet skill's output conventions before writing up.Two final verification passes before writing up — review-ladder ref reachability and a realistic agentwrap auth-failure rendering:All verification complete. Writing the job result.Review complete. Full report: `review/jobs/review-cli-dispatch-exit-contract-security/result.md`.

**Findings**

**F1 (P2, confirmed) — Substring classification corrupts the exit contract in both directions.** The sprint flow/execute/review ladders classify by `strings.Contains(err.Error(), "runtime")` / `"failed tasks"` (sprint_commands.go:269, :360-364, :534) before the typed `mapSprintError` fallback. Reproduced live on the frozen binary:
- `sprint benign-metrics 01 flow --to plan --dry-run` in a workspace containing `projects/runtime-metrics` → **exit 6** (`project reference … available: runtime-metrics`) instead of 5; sibling verb `status` on the same shape exits 5.
- Inverse: provider auth/rate-limit/model-unavailable failures propagate as raw `*agentwrap.SDKError` (run_control.go:342, service.go:608) whose text lacks "runtime" → **exit 4** (`validation.workspace`) for genuine runtime failures — both contradicting docs/cli-reference.md's exit table. Affects dry-run too; hits stable-listed `review --json`. No test drives these branches (the one flow→ExitRuntime pin exercises a pre-ladder classed error). Fix direction: typed sentinels/`errors.As` only.

**F2 (P3, high confidence on facts) — `study init` clone URLs unvalidated + unsanitized remote output to terminal.** URLs go straight into `git clone` argv (init_clone.go:44-50). Leading-dash option injection (`--upload-pack=<cmd>`) verified as a git primitive, but honestly **not executable end-to-end today**: the repo argument is always a nonexistent dest, so git aborts first (verified twice through the binary). Separately, remote-controlled git output reaches stderr without control-char stripping (redactGitOutput lacks what displaySafe applies elsewhere), sink at study_commands.go:1477.

**Defended non-issues:** serve listen validation (tight), TTY-less confirmations (conservative EOF), code/prompt `--output` asymmetry (operator capability vs containment), secret redaction sinks, openBrowser exec, durable acceptance ordering.