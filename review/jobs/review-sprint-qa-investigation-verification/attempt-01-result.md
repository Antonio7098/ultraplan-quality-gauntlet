The repo has moved past the job's frozen commit (`c6f01cf` → two later QA-fix commits → HEAD). My review target is the frozen commit, so I'll export its exact bytes to a scratch dir rather than review the drifted working tree.Frozen tree exported. Now reading the remaining primary sources at the freeze point.Now the frozen tests.Dead test scaffolding confirmed (`qaRetryRuntime`/`qaOutputRetryRuntime` unused at freeze), and no end-to-end `RunQA` test. Now the app wiring: fence minting, progress consumption, cancel ownership.The fence heartbeats the lease on every publish check. Now the critical question: what does `Heartbeat` do once a run is cancelled or terminal — because that decides whether the cancelled-state publication can succeed at all.Heartbeat survives cancellation acknowledgement (only terminal outcomes kill it), so the cancelled-state publication path holds for normal flows. Now the remaining wiring: CancelQA ownership, settings enforcement, `targetIdentity`, flow-state locking, and whether `Retryable` is consumed anywhere.Post-freeze commits corroborate both major candidates (follow-up re-synthesis trap; missing evidence-window identity guard). Now closing out the remaining verification threads.Baseline green at the freeze. Now the decisive repro for the follow-up re-synthesis trap:Candidate A confirmed by execution against frozen bytes. Now verifying the remaining smaller claims before writing up.All evidence gathered. Checking artifact conventions, then writing the deliverable.Sibling reviewer artifacts exist (adversarial/correctness lenses) — per doctrine I have not read them. Writing my verification-lens deliverable.Review complete. Deliverables written (target and workspace repos untouched; the pre-existing `skills.go` dirt in ultraplan-go predates this session).

**Deliverables**
- `review/surfaces/review-sprint-qa-investigation-verification.md` — 9 findings + 5 defended non-issues, all against frozen `c6f01cf` bytes
- `review/jobs/review-sprint-qa-investigation-verification/result.md` — job summary

**Key results**

| ID | Sev/Conf | Finding |
|----|----------|---------|
| F1 | P1 confirmed | Post-follow-up re-synthesis proposes shards that were never run; `hydrateQASynthesisFollowUps` then blocks the fully-executed attempt. **Proven by executed repro against frozen code** (`inconclusive` theory in a follow-up shard → re-synthesis proposes 4, 2 unrun → attempt Blocked). Corroborated by post-freeze fix `4841b74`. Resume re-runs follow-ups from scratch, so the cost trap repeats. |
| F2 | P2 high | Map fence is test-only: production installs only the writer fence, so every shard pays 3 full `QAMap` rebuilds + 5 whole-tree identity hashes (~18 git spawns/shard) charged to `RunTimeout`; the suite always fakes the fence — fake-only confidence. |
| F3 | P2 high | Evidence-producing window has no target-identity guard between last shard check and report/adjudication; corroborated by post-freeze fix `208c9d0`. |
| F4 | P2 high-mech | Unleased `Service.Status` refresh does read-modify-write of flow-state.json and can revert the leased QA pointer; dashboards trigger it routinely during runs. |
| F5–F9 | P3 | Terminal-failure publish drops computed synthesis ref; planted symlink misclassified as `persistence_failure` (exit 6 vs 5); progress cap keeps oldest events and drops completion under documented budgets; resume with mismatched attempt silently goes fresh; RecoverQA marks Stale on transient map errors. |

Refuted hypotheses worth noting: heartbeat-during-cancel does **not** break cancelled-state publication (`AcknowledgeCancellation` keeps the run non-terminal until `FinishOperation`), and the chmod walk is bounded at `verification/` — it cannot touch shared sprint dirs.